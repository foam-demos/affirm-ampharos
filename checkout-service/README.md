# Checkout Service

Orchestrates the consumer checkout flow including cart validation, SKU verification, merchant callbacks, and payment processing. Built with Kotlin/Spring Boot, communicates via gRPC. Deployed on AWS EKS.

## Running Locally
```bash
./gradlew bootRun
```