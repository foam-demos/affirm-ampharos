# Merchant Integration API

Enterprise partner APIs powering checkout integrations, merchant onboarding, and custom solutions. Kotlin/Spring Boot with gRPC endpoints. Handles integrations with Shopify, Magento, Salesforce Commerce Cloud, and custom merchant implementations.

## Running Locally
```bash
./gradlew bootRun --args='--spring.profiles.active=local'
```